const InfoBadge = ({ text }) => {
  return (
    <span
      className="
      px-3
      py-1
      bg-gray-100
      rounded-full
      text-sm
      "
    >
      {text}
    </span>
  );
};

export default InfoBadge;